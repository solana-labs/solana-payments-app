import React from 'react';
import { Wallet } from './Wallet';

const BuyButton = () => {
  return (
    <button className="btn w-full bg-black text-white py-4 pt-3 text-base rounded-md shadow-lg font-semibold flex justify-center items-center">
      Buy Now
    </button>
  );
};

export default BuyButton;
