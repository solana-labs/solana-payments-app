import React from 'react';

const BuyButton = () => {
  return (
    <button className="max-w-full bg-black text-white py-4 px-0 pt-3 text-base rounded-md shadow-lg font-semibold flex justify-center items-center">
      Buy Now
    </button>
  );
};

export default BuyButton;
