const API_BASE_URL = 'https://ehfytczade.execute-api.us-east-1.amazonaws.com';
const API_ENDPOINTS = {
    install: `${API_BASE_URL}/install`,
    redirect: `${API_BASE_URL}/redirect`,
    payment: `${API_BASE_URL}/payment`,
    // ... (define the rest of your endpoints here)
};
